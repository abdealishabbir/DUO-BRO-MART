const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api';
export async function checkout(access: string, data: Record<string, unknown>) { const response = await fetch(`${API_URL}/orders/checkout/`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${access}` }, body: JSON.stringify(data) }); const result = await response.json(); if (!response.ok) throw new Error(result.detail || Object.values(result).flat().join(' ')); return result as { code: string; total: string; status: string }; }
export async function trackOrder(code: string) { const response = await fetch(`${API_URL}/orders/track/${code}/`); if (!response.ok) throw new Error('Order not found.'); return response.json() as Promise<{ code: string; status: string; delivery_method: string }>; }
