import { LogOut, PackagePlus, Store } from 'lucide-react';

export default function VendorPanel({ name, onLogout }: { name: string; onLogout: () => void }): JSX.Element {
  return <main className="min-h-screen bg-[#FBF7EF] px-5 py-10 text-[#101A2E] sm:px-8"><section className="mx-auto max-w-5xl"><header className="flex flex-wrap items-center justify-between gap-5 border-b border-[#e8ddd2] pb-6"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-[#F2A93B]"><Store size={21} /></div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#E0912A]">Vendor panel</p><h1 className="text-2xl font-extrabold">Welcome, {name}</h1></div></div><button onClick={onLogout} className="inline-flex items-center gap-2 rounded-xl border border-[#d6c9bb] bg-white px-4 py-2.5 text-sm font-bold hover:bg-neutral-50"><LogOut size={16} /> Log out</button></header><div className="mt-10 rounded-2xl border border-[#e8ddd2] bg-white p-8 shadow-sm"><PackagePlus className="mb-4 text-[#E0912A]" size={32} /><h2 className="text-xl font-bold">Your seller workspace is ready</h2><p className="mt-2 max-w-xl text-sm leading-relaxed text-[#5B6B85]">Product submission, inventory, deals, and order management will be connected in Phase 4. Only approved vendor accounts will be permitted by the planned API.</p></div></section></main>;
}
